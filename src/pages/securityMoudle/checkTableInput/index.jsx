import React, { Component } from 'react';

class CheckTableInput extends Component {
    render() {
        return (
            <div>
                检查表录入，显示列表，选择一项，进入填写提交页面
            </div>
        )
    }
}
export default CheckTableInput;